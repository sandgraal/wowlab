[project]
name = "wowlab-core"
version = "0.1.0"
description = "Local-only library and CLI over a World of Warcraft install"
requires-python = ">=3.12"
license = "Apache-2.0"
# Later tickets add psutil when they first need it (docs/LAB_PLAN.md §6).
# Anything beyond that list needs a line in the PR body.
dependencies = [
  "pydantic>=2.11",
  "typer>=0.16",
  "httpx>=0.28",
  "platformdirs>=4.3",
]

[project.scripts]
wowlab = "wowlab_core.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/wowlab_core"]
